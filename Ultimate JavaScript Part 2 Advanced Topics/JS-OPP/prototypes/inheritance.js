
// Inheritance
/**
 * Inheritence
 * enables an object to take on the properties and methods
 * of an another object.
 */

// Inheritance in his classical term
/**
 * For e.g: We have an application with a Cricle calss which has one ,method
 *  'computeOptimumLocation()'
 * the next day we decide to add another class 'Square', which needs this
 * same method 'computeOptimumLocation()'
 * 
 * We define a new class call 'Shape' and put the 'computeOptimumLocation'
 * in it.
 */

/**
 * Inheritance Terms:
 *  Base class or super class or parent class
 *  In constrast we refer
 */
