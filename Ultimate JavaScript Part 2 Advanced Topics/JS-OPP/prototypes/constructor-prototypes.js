
// constructor function
function Circle(radius) {
  this.radius = radius;
}

const circle = new Circle(10);

// to get the prototype of an object
// Object.getPrototypeOf(myObj); // returns [[Prototype]] 

Circle.prototype
